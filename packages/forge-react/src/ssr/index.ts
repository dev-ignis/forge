/**
 * SSR utilities for React components
 * 
 * Note: All Forge React components are now unified and work in both SSR and client environments.
 * This module is provided for compatibility and exports the same components as the main index.
 */

// Re-export everything from main index for SSR compatibility
export * from '../index';